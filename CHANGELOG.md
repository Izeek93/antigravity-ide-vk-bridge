# Changelog (Antigravity IDE VK Bridge)

Все значимые изменения проекта **Antigravity IDE VK Bridge** фиксируются в этом документе.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и проект придерживается [Семантического версионирования (SemVer)](https://semver.org/lang/ru/).

## [1.2.0] - 2026-09-04

### Added
- **Отказоустойчивый Watchdog-супервизор (`bridge_supervisor.py`):**
  - Непрерывный мониторинг дочернего процесса LongPoll-демона.
  - Автоматический перезапуск за `<0.5с` при падениях сети или аварийных сбоях.
  - Защита от шторма рестартов (экспоненциальный backoff от 0.5с до 5с).
  - Работает в пользовательском пространстве без необходимости прав администратора (Zero UAC).
  - Экспорт метрик надёжности в `supervisor_status.json`.
- **Stdio FastMCP сервер (`vk_mcp_server.py`):**
  - Нативная интеграция с Antigravity IDE через протокол Model Context Protocol.
  - 5 нативных инструментов: `vk_send_message`, `vk_send_voice`, `vk_send_photo`, `vk_post_to_wall`, `vk_get_status`.
  - Строгая изоляция `stdout` под JSON-RPC фреймы FastMCP, логирование перенаправлено в `stderr`.
- **Официальная стратегическая дорожная карта (`ROADMAP.md`):**
  - Структурированный публичный план развития проекта на 2026 год (Фазы 1–5: от LongPoll до Webhook Gateway и Multi-Topics).
- **Пакет автоматизированного тестирования (`tests/test_vk_mcp.py`):**
  - 100% покрытие базовых сценариев инициализации, валидации ввода и диагностики.

---

## [1.1.1] - 2026-09-04

### Added
- **Интерактивный терминальный статус-трекер (`status_tracker.py`):**
  - Динамическая статус-карточка в чате VK с визуальным прогресс-баром и таймингом.
  - Пошаговые индикаторы выполнения (`STT -> IDE -> TTS -> VK`).
  - Честный подсчет контекстных, входящих и исходящих токенов (`Токены: 🧠 Контекст | ⬇️ Вход | ⬆️ Ответ | Σ Всего`).
- **Управление пульсом активности (`heartbeat_manager.py`):**
  - Фоновый воркер удержания статуса «набирает сообщение...» каждые 4.5с.
  - Гарантированная мгновенная остановка пульса при доставке ответа или экстренном сигнале SOS.
- **Гарантия единственного экземпляра (`ensure_single_instance`):**
  - Автоматическое обнаружение и завершение старых экземпляров демонов моста через `psutil`.
- **Всеядный обработчик экстренного прерывания (SOS):**
  - Распознавание кириллицы и команд отмены: `сос`, `/сос`, `стоп`, `сброс`, `отмена`, кнопка `🚨 SOS`.
  - Принудительное мгновенное аппаратное завершение (`kill`) зависших процессов `omnivoice-tts.exe` и `ffmpeg.exe`.

### Changed
- **Оптимизация скорости TTS:**
  - Голосовые ответы оптимизированы под краткие выжимки (15–25с аудио), ускоряя нейро-синтез в 5 раз.
- **Удаление GUI модальных окон:**
  - Работа с внешними ресурсами и документацией переведена на автономные консольные скрипты без модальных окон операционной системы.

---

## [1.1.0] - 2026-09-03

### Added
- **Полная изоляция от других проектов (Zero Coupling):**
  - Очередь `inbox.json` и файл блокировки `inbox.lock` перенесены строго внутрь каталога `vk-bot/`.
  - Устранена любая зависимость от файлов и путей соседнего Telegram-моста.
  - Создано собственное изолированное виртуальное окружение `vk-bot/venv`.
- **Автономный слушатель очереди (`bridge_receiver.py`):**
  - Локальный файловый приёмник для моментального пробуждения агента в IDE без открытых сетевых портов.
  - Защита от коллизий ввода-вывода (`try/except`).
- **Модульный маршрутизатор команд (`command_router.py`):**
  - Изоляция логики команд (`/limits`, `/screen`, `/tasks`, `/voice`, `/status`, `/help`, `/start`).
  - Поддержка интерактивных аппрувов действий в IDE (`/approve`, `/reject`, `да`, `нет`).
- **Универсальный клиент и CLI (`send_vk.py`):**
  - Поддержка отправки сообщений, фотографий, документов и голосовых ответов через CLI.
  - Автоматический сплиттер длинных сообщений (>4000 символов) для соблюдения лимитов VK API.
- **Очистка репозитория:**
  - Удалено более 45 устаревших отладочных скриптов (`send_reply*.py`) и временных медиафайлов.
  - Введена изолированная директория `media/` с автоматической ротацией файлов старше 24 часов.

### Changed
- **Рефакторинг демона `vk_bridge.py`:**
  - Облегчен до чистого транспорта LongPoll.
  - Добавлен порог сетевых ошибок (`consecutive_errors >= 5`) для устранения ложных алертов при кратковременных SSL/HTTP сбоях LongPoll.
- **Очередь сообщений (`queue_manager.py`):**
  - Переведена на `portalocker.Lock` для надёжной межпроцессной OS-level блокировки в Windows.

---

## [1.0.0] - 2026-08-30

### Added
- Базовый двусторонний LongPoll мост между VK сообществом и Antigravity IDE.
- Локальная транскрибация аудио через Faster-Whisper.
- Синтез речи OmniVoice.
- Интерактивная клавиатура VK.
